{
  "OpenAiSettings": {
    "ApiToken": "",
    "ModelName": "gpt-3.5-turbo",
    "MaxTokens": null,
    "Temperature": 0.5,
    "SystemPrompt": "You are a a coding master.",
    "Prompt": "[INST]check the inline comments to the code and replace them within the code if you think they are not in line with the code. Replace the text within the <summary></summary>, <param></param> and <returns></returns> blocks with the newly generated comment. Return the newly generated comments and the method line it belongs to only, place the method line underneath the comment. Do not add or change any code. Do not add any comments. Do not add any text.[/INST]\n${code}"
  },
  "Settings": {
    "SupportedContentTypes": ["application/zip"],
    "allowedFileTypes": [ ".ts" ],
    "TempFolderPath": "../../../Testfiles/",
    "OutputFolderPath": "../../../Testfiles/Output/",
    "ServiceBusUrl": "http://localhost:5195/uploadZip",
    "SqLiteDbPath": "",
    "SqLiteDbName": "aia.db"
  }
}
